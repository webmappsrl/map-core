{
  "name": "map-core",
  "version": "1.2.0",
  "author": "Webmapp",
  "license": "ISC",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "surge-doc": "./node_modules/surge/lib/cli.js --project ./documentation --domain http://doc.map-core.surge.sh/",
    "surge-cov": "./node_modules/surge/lib/cli.js --project ./coverage/map-core --domain http://cov.map-core.surge.sh/",
    "release:major": "changelog -M && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version major && git push origin && git push origin --tags",
    "release:minor": "changelog -m && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version minor && git push origin && git push origin --tags",
    "release:patch": "changelog -p && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version patch && git push origin && git push origin --tags",
    "compodoc": "npx compodoc -p tsconfig.doc.json",
    "serve:compodoc": "npx compodoc -p tsconfig.doc.json  -s -d ./documentation",
    "coverage": "ng test --no-watch --code-coverage",
    "compodoc:build": "compodoc -p tsconfig.doc.json",
    "compodoc:build-and-serve": "compodoc -p tsconfig.doc.json -s",
    "compodoc:serve": "compodoc -s"
  },
  "private": true,
  "dependencies": {
    "localforage": "^1.10.0",
    "ol": "7.1.0",
    "ol-ext": "^4.0.11",
    "rxjs": "~7.8.2",
    "tslib": "^2.8.1",
    "zone.js": "~0.15.1"
  },
  "peerDependencies": {
    "@angular/animations": "~20.3.16",
    "@angular/common": "~20.3.16",
    "@angular/compiler": "^20.3.16",
    "@angular/core": "^20.3.16",
    "@angular/forms": "^20.3.16",
    "@angular/platform-browser": "^20.3.16",
    "@angular/platform-browser-dynamic": "^20.3.16",
    "@angular/router": "^20.3.16",
    "@ionic/angular": "^8.7.16"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^20.3.14",
    "@angular/cli": "^20.3.14",
    "@angular/compiler-cli": "^20.3.16",
    "@compodoc/compodoc": "^1.1.19",
    "@types/jasmine": "~5.1.0",
    "@types/node": "^12.11.1",
    "@types/ol": "^6.5.3",
    "generate-changelog": "^1.8.0",
    "jasmine-core": "~5.1.1",
    "karma": "^6.3.17",
    "karma-chrome-launcher": "~3.1.0",
    "karma-coverage": "~2.0.3",
    "karma-jasmine": "~5.0.0",
    "karma-jasmine-html-reporter": "~2.1.0",
    "ng-packagr": "^20.3.0",
    "surge": "^0.23.1",
    "typescript": "~5.8.0"
  }
}
